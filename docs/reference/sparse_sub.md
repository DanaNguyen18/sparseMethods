# Sparse subtraction

Subtracts two sparse vectors

## Usage

``` r
sparse_sub(x, y)

# S4 method for class 'sparse_numeric,sparse_numeric'
sparse_sub(x, y)

# S4 method for class 'sparse_numeric,sparse_numeric'
e1 - e2
```

## Arguments

- x:

  A 'sparse_numeric' object

- y:

  A 'sparse_numeric' object

- e1:

  First operand (sparse_numeric)

- e2:

  Second operand (sparse_numeric)

## Value

A 'sparse_numeric' object

## Functions

- `sparse_sub(x = sparse_numeric, y = sparse_numeric)`: Subtracts two
  sparse vectors

- `e1 - e2`: '-' symbol for sparse_numeric vectors
