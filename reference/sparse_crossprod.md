# Sparse cross production

Computes the dot product of two sparse vectors

## Usage

``` r
sparse_crossprod(x, y)

# S4 method for class 'sparse_numeric,sparse_numeric'
sparse_crossprod(x, y)
```

## Arguments

- x:

  A 'sparse_numeric' object

- y:

  A 'sparse_numeric' object

## Value

A 'sparse_numeric' object

## Functions

- `sparse_crossprod(x = sparse_numeric, y = sparse_numeric)`: Cross
  products two sparse vectors
