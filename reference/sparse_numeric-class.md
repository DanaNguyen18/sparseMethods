# Sparse Numeric Vector Class

Defining an S4 class for storing numeric vectors by holding only nonzero
values and their positions

## Slots

- `value`:

  A numeric vector of nonzero values

- `pos`:

  Integer vector giving positions of each nonzero value

- `length`:

  Integer specifying length of the vector
